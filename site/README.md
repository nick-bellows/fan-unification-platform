# fanuni dashboards

Evidence.dev project that builds the dashboard site from the warehouse marts.
See the repository README for the full context; CI builds and deploys this to
GitHub Pages from a real pipeline run.

Local: `npm install && npm run sources && npm run dev` (compose stack up,
pipeline run first).
