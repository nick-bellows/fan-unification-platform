# Keeps the SHA-pinned action refs in .github/workflows current. Python pins
# live in constraints.txt and are regenerated deliberately (see its header);
# the npm tree is dispositioned in docs/future-work.md.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    labels: [dependencies]
